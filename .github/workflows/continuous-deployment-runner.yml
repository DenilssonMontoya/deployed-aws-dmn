name: CD
on:
  push:
    branches: [master-runner]
env:
  REPOSITORY: ${{ github.repository }}
  GHCR_URL: ghcr.io # GitHub Container Registry
  AWS_ECR_URL: ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ secrets.AWS_REGION_RUNNER }}.amazonaws.com
jobs:
  cd:
    name: Build & Push GitHub Package & Deploy on Render Docker Image
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: 21
      - name: Get version from pom.xml
        id: get-version
        run: |
          echo "VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)" >> $GITHUB_ENV
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.GHCR_URL }} # GitHub Container Registry
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PERSONAL_ACCESS_TOKEN }}
      - name: Build Docker image
        run: docker build  -t $REPOSITORY:$VERSION .
      - name: Tag and Push on GitHub Packages
        run: |
          LOWER_URL=$(echo "$GHCR_URL/$REPOSITORY" | tr '[:upper:]' '[:lower:]')          
          docker tag $REPOSITORY:$VERSION $LOWER_URL:$VERSION
          docker tag $REPOSITORY:$VERSION $LOWER_URL:latest
          docker push $LOWER_URL:$VERSION
          docker push $LOWER_URL:latest

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION_RUNNER }}
      - name: Login to AWS
        uses: aws-actions/amazon-ecr-login@v2
      - name: Tag and Push AWS ECR
        run: |
          LOWER_URL_REPO=$(echo "$REPOSITORY" | tr '[:upper:]' '[:lower:]')                    
          docker tag $LOWER_URL_REPO:$VERSION $AWS_ECR_URL/$LOWER_URL_REPO:latest
          docker push $AWS_ECR_URL/$LOWER_URL_REPO:latest